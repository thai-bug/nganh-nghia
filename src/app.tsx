const App = () => {
  return <></>;
  // return (
  //   <div className="container mx-auto p-4">
  //     <Players players={players} scores={scores} />

  //     {currentRound === 1 && (
  //       <Round1
  //         hashtags={round1Hashtags}
  //         onComplete={() => setCurrentRound(2)}
  //       />
  //     )}

  //     {currentRound === 2 && (
  //       <Round2 puzzle={crosswordData} onComplete={() => setCurrentRound(3)} />
  //     )}

  //     {currentRound === 3 && (
  //       <Round3
  //         packages={round3Packages}
  //         onComplete={() => setCurrentRound(null)}
  //       />
  //     )}
  //   </div>
  // );
};

export default App;
